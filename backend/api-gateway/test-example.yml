server:
  port: 8083

spring:
  application:
    name: api-gateway-test
  cloud:
    gateway:
      mvc:
        routes:
          - id: example
            uri: http://example.com
            predicates:
              - Path=/**
