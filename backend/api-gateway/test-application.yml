server:
  port: 8082

spring:
  application:
    name: api-gateway-test
  cloud:
    gateway:
      mvc:
        routes:
          - id: product-service
            uri: lb://PRODUCT-SERVICE
            predicates:
              - Path=/api/products/**

management:
  endpoints:
    web:
      exposure:
        include: "*"
